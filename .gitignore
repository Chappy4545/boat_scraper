# データベース（大容量のためgit管理外）
data/boatrace.db

# キャッシュ・一時ファイル
data/raw/
data/external/
data/interim/
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
.eggs/

# ログ
logs/*.log
logs/*.log.zip

# 環境
.env
.venv/
venv/
*.env

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude内部ファイル
.claude/

# CatBoost学習ログ（予測時にも書き込まれるため除外）
catboost_info/

# デバッグ用一時ファイル
pay_page_debug.html

# スクリプト（自動生成）
logs/collect_odds_loop.sh
run_collect_then_train.sh

# 検証用モデル・退避コピー (実験成果物。本番モデルのみ管理する)
data/processed/models_backup_*/
data/processed/models_cutoff_*/
data/processed/models_prod_backup_*/
scratch_*

# クラウド予測が使う当日ぶんの使い捨てDB（コミットしない）
cloud_scratch.db
data/cloud_*.db
data/processed/models_start_*/
data/.train.lock

# 通知先の webhook URL。投稿権そのものなのでコミットしない
data/.notify_webhook
